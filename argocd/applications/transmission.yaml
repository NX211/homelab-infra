---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: transmission
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: transmission=lscr.io/linuxserver/transmission
    # LinuxServer transmission tags carry an -rN revision (e.g. 4.1.2-r0-ls348),
    # not valid semver. Track by build date, restricted to clean release tags.
    argocd-image-updater.argoproj.io/transmission.update-strategy: newest-build
    argocd-image-updater.argoproj.io/transmission.allow-tags: regexp:^[\d.]+-r\d+-ls\d+$
    argocd-image-updater.argoproj.io/transmission.helm.image-name: image.repository
    argocd-image-updater.argoproj.io/transmission.helm.image-tag: image.tag
    argocd-image-updater.argoproj.io/write-back-method: git:secret:argocd/git-creds
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/NX211/homelab-infra.git
    targetRevision: HEAD
    path: helm-charts/transmission
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
