---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authelia-manifests-tls
  namespace: default
spec:
  secretName: authelia-manifests-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - login.authoritah.com
