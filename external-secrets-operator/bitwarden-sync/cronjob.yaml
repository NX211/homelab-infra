---
# CronJob to sync Bitwarden secrets to Kubernetes
apiVersion: batch/v1
kind: CronJob
metadata:
  name: bitwarden-sync
  namespace: secrets-vault
spec:
  # Run every 15 minutes
  schedule: "*/15 * * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: bitwarden-sync
        spec:
          serviceAccountName: bitwarden-sync
          restartPolicy: OnFailure
          containers:
            - name: sync
              # Pinned by digest so the base image can't change under us.
              image: debian:bookworm-slim@sha256:3783cc01769c7b2b1b83a5c5ad96c815348e28ed7da68e2e3687004faa906251
              command: ["/bin/bash"]
              args: ["/scripts/sync.sh"]
              env:
                - name: BWS_ACCESS_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: bitwarden-credentials
                      key: token
              securityContext:
                allowPrivilegeEscalation: false
                seccompProfile:
                  type: RuntimeDefault
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
              volumeMounts:
                - name: sync-script
                  mountPath: /scripts
          volumes:
            - name: sync-script
              configMap:
                name: bitwarden-sync-script
                defaultMode: 0755
