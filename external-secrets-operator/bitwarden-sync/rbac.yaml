---
# ServiceAccount for Bitwarden sync job
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bitwarden-sync
  namespace: secrets-vault
---
# Role to manage secrets in secrets-vault namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: bitwarden-sync
  namespace: secrets-vault
rules:
  # No "delete": the sync only ever creates/updates secrets (kubectl apply), so
  # a compromised job cannot wipe the secret store.
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "create", "update", "patch"]
---
# RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: bitwarden-sync
  namespace: secrets-vault
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: bitwarden-sync
subjects:
  - kind: ServiceAccount
    name: bitwarden-sync
    namespace: secrets-vault
