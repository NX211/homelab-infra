---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "bazarr.fullname" . }}-tls
  labels:
    {{- include "bazarr.labels" . | nindent 4 }}
spec:
  secretName: {{ include "bazarr.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
