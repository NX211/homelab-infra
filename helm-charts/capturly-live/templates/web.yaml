---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "capturly-live.fullname" . }}-web
  labels:
    app.kubernetes.io/name: capturly-live-web
    {{- include "capturly-live.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: capturly-live-web
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: capturly-live-web
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      enableServiceLinks: false
      containers:
      - name: web
        image: "{{ .Values.web.image.repository }}:{{ .Values.web.image.tag }}"
        imagePullPolicy: {{ .Values.web.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.web.service.port }}
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 1
        resources:
          {{- toYaml .Values.web.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "capturly-live.fullname" . }}-web
  labels:
    app.kubernetes.io/name: capturly-live-web
    {{- include "capturly-live.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.web.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: capturly-live-web
    app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "capturly-live.fullname" . }}-web
  labels:
    app.kubernetes.io/name: capturly-live-web
    {{- include "capturly-live.labels" . | nindent 4 }}
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`{{ .Values.web.host }}`)
    kind: Rule
    services:
    - name: {{ include "capturly-live.fullname" . }}-web
      port: {{ .Values.web.service.port }}
  tls:
    secretName: {{ include "capturly-live.fullname" . }}-web-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "capturly-live.fullname" . }}-web-tls
  labels:
    app.kubernetes.io/name: capturly-live-web
    {{- include "capturly-live.labels" . | nindent 4 }}
spec:
  secretName: {{ include "capturly-live.fullname" . }}-web-tls
  issuerRef:
    name: {{ .Values.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.web.host }}
