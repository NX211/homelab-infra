---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "it-tools.fullname" . }}-tls
  labels:
    {{- include "it-tools.labels" . | nindent 4 }}
spec:
  secretName: {{ include "it-tools.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
