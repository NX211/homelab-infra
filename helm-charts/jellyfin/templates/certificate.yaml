---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "jellyfin.fullname" . }}-tls
  labels:
    {{- include "jellyfin.labels" . | nindent 4 }}
spec:
  secretName: {{ include "jellyfin.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
