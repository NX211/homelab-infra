---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "karakeeper.fullname" . }}-tls
  labels:
    {{- include "karakeeper.labels" . | nindent 4 }}
spec:
  secretName: {{ include "karakeeper.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
