---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "karakeeper.fullname" . }}
  labels:
    {{- include "karakeeper.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "karakeeper.selectorLabels" . | nindent 4 }}
