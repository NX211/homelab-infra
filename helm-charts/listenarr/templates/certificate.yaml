---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "listenarr.fullname" . }}-tls
  labels:
    {{- include "listenarr.labels" . | nindent 4 }}
spec:
  secretName: {{ include "listenarr.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
