---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarr-uhd.fullname" . }}
  labels:
    {{- include "radarr-uhd.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "radarr-uhd.selectorLabels" . | nindent 4 }}
