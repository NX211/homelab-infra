---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "sabnzbd.fullname" . }}-tls
  labels:
    {{- include "sabnzbd.labels" . | nindent 4 }}
spec:
  secretName: {{ include "sabnzbd.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
