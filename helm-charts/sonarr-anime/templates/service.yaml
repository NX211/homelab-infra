---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarr-anime.fullname" . }}
  labels:
    {{- include "sonarr-anime.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "sonarr-anime.selectorLabels" . | nindent 4 }}
