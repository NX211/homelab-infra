---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "sonarr-uhd.fullname" . }}-tls
  labels:
    {{- include "sonarr-uhd.labels" . | nindent 4 }}
spec:
  secretName: {{ include "sonarr-uhd.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
