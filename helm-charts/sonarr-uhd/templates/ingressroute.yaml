---
{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "sonarr-uhd.fullname" . }}
  labels:
    {{- include "sonarr-uhd.labels" . | nindent 4 }}
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`{{ .Values.ingress.host }}`)
    kind: Rule
    middlewares:
    - name: authelia-forwardauth
      namespace: default
    - name: default-headers
      namespace: default
    services:
    - name: {{ include "sonarr-uhd.fullname" . }}
      port: {{ .Values.service.port }}
  tls:
    secretName: {{ include "sonarr-uhd.fullname" . }}-tls
{{- end }}
