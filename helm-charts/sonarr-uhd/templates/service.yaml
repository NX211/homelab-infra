---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarr-uhd.fullname" . }}
  labels:
    {{- include "sonarr-uhd.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "sonarr-uhd.selectorLabels" . | nindent 4 }}
