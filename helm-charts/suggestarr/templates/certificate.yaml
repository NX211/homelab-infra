---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "suggestarr.fullname" . }}-tls
  labels:
    {{- include "suggestarr.labels" . | nindent 4 }}
spec:
  secretName: {{ include "suggestarr.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
