---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dispatchr-staging.fullname" . }}-tls
  labels:
    {{- include "dispatchr-staging.labels" . | nindent 4 }}
spec:
  secretName: {{ include "dispatchr-staging.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
