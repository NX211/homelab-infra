---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispatchr-staging.fullname" . }}
  labels:
    {{- include "dispatchr-staging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "dispatchr-staging.selectorLabels" . | nindent 4 }}
