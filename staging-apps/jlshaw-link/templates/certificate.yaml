---
{{- if .Values.ingress.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "jlshaw-staging.fullname" . }}-tls
  labels:
    {{- include "jlshaw-staging.labels" . | nindent 4 }}
spec:
  secretName: {{ include "jlshaw-staging.fullname" . }}-tls
  issuerRef:
    name: {{ .Values.ingress.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
{{- end }}
