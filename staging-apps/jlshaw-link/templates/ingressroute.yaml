---
{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "jlshaw-staging.fullname" . }}
  labels:
    {{- include "jlshaw-staging.labels" . | nindent 4 }}
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`{{ .Values.ingress.host }}`)
    kind: Rule
    services:
    - name: {{ include "jlshaw-staging.fullname" . }}
      port: {{ .Values.service.port }}
  tls:
    secretName: {{ include "jlshaw-staging.fullname" . }}-tls
{{- end }}
