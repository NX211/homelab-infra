---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "jlshaw-staging.fullname" . }}
  labels:
    {{- include "jlshaw-staging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "jlshaw-staging.selectorLabels" . | nindent 4 }}
